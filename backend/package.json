{
  "name": "delegate-voting-indexer",
  "version": "1.0.0",
  "description": "Indexing service for ve-governance delegate voting breakdowns",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src --ext .ts",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "ethers": "^6.10.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@vercel/node": "^3.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.1.0",
    "tsx": "^4.7.0"
  },
  "engines": {
    "node": "20.x"
  }
}
